# Stripe publishable key (safe to expose in frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Backend API base (without trailing slash)
# In dev the Vite proxy handles /api so you can leave this empty
VITE_API_URL=
